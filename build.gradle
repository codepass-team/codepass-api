allprojects {
    group = 'com.codepass'
    version = '0.0.1'
}

subprojects {
    apply plugin: 'java'

    sourceCompatibility = '14'

    repositories {
        mavenLocal()
        mavenCentral()
    }

    dependencies {
        testCompile "junit:junit:4.8.2"
    }
}
